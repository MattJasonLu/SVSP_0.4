<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jdlink.mapper.dictionary.SecondaryCategoryMapper" >

    <select id="list" resultType="com.jdlink.domain.Dictionary.SecondaryCategory">
        select * from d_secondarycategory;
    </select>

    <select id="getByCode" resultType="com.jdlink.domain.Dictionary.SecondaryCategory">
        select * from d_secondarycategory where code=#{code} limit 0, 1;
    </select>

</mapper>